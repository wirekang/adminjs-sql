version: '3'
name: adminjs-sql-example
services:
  db:
    image: mysql:latest
    container_name: adminjs-sql-example-db
    ports:
      - 33301:3306
    environment:
      MYSQL_USER: user
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: database
